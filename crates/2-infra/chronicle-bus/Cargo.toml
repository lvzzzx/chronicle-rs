[package]
name = "chronicle-bus"
version = "0.1.0"
edition = "2021"

[dependencies]
chronicle-core = { path = "../../1-primitives/chronicle-core" }
anyhow = "1"
thiserror = "1"
# For discovery/fswatch (implementation pending)
# notify = "6" 

[dev-dependencies]
tempfile = "3"