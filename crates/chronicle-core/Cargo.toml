[package]
name = "chronicle-core"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
description = "Chronicle-style low-latency persisted messaging queue"

[lib]
path = "src/lib.rs"

[dependencies]
crc32fast = "1"
libc = "0.2"
memmap2 = "0.9"

[dev-dependencies]
tempfile = "3"
