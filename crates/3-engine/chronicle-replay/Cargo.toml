[package]
name = "chronicle-replay"
version = "0.1.0"
edition = "2021"

[dependencies]
chronicle = { path = "../../chronicle" }
anyhow = "1"

[dev-dependencies]
criterion = "0.5"
tempfile = "3"

[[bench]]
name = "warm_start"
harness = false
